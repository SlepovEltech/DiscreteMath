#include<stdio.h>
#include "../struct.h"
#include "lims.h"


P* initPolynom(int power){
// PAIN
int i;
P* output = (P*)malloc(sizeof(P));
output->c = (Q*)malloc(power*sizeof(Q));
output->m = power;
for(i=0;i<m;i++)
    
}


int main(){


    return 0;
}